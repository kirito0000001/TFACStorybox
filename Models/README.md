# Models

This folder is reserved for data models moved out of `MainWindow.xaml.cs` during the MVVM migration.

Step 1 intentionally does not move existing records yet. Models will be migrated in small groups after the MVVM infrastructure builds cleanly.
